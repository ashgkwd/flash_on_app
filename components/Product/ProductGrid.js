import * as React from "react";
import {
  Image,
  Platform,
  StyleSheet,
  Text,
  TouchableOpacity,
  View
} from "react-native";

function ProductGrid({ orientation, filter }) {
  return (
    <View>
      <Text>Product Grid {orientation}</Text>
    </View>
  );
}

export default ProductGrid;
